import path from 'node:path'; import {randomUUID} from 'node:crypto'; import {JsonRepository} from './jsonRepository.js'; import type {AppDefinition,Note,Notification,Settings} from '../../shared/types/domain.js';
export const defaultSettings:Settings={theme:'dark',accent:'#7c6cff',wallpaper:'aurora',fontScale:1,dockPosition:'bottom',animation:'full',reducedMotion:false};
export class DataService { settings:JsonRepository<Settings>;notes:JsonRepository<Note[]>;notifications:JsonRepository<Notification[]>;state:JsonRepository<{pinnedApps:string[]}>;readonly apps:AppDefinition[]=[['files','Files','▰','Browse and manage local files'],['editor','Editor','✎','Edit plain-text documents'],['notes','Notes','◫','Capture persistent notes'],['terminal','Terminal','›_','Run safe OS commands'],['monitor','Monitor','⌁','Inspect this session'],['calculator','Calculator','＋','Calculate with history'],['settings','Settings','⚙','Personalize FluidaOS'],['wallpapers','Wallpapers','◉','Choose a desktop background']].map(([id,name,icon,description])=>({id,name,icon,description}));constructor(root=path.resolve(process.cwd(),'os_storage/.fluida')){this.settings=new JsonRepository(path.join(root,'settings.json'),defaultSettings);this.notes=new JsonRepository(path.join(root,'notes.json'),[]);this.notifications=new JsonRepository(path.join(root,'notifications.json'),[{id:'welcome',title:'Welcome to FluidaOS',message:'Your workspace is ready.',read:false,createdAt:new Date().toISOString()}]);this.state=new JsonRepository(path.join(root,'state.json'),{pinnedApps:['files','notes','terminal','settings']});}async saveSettings(patch:Partial<Settings>){return this.settings.write({...await this.settings.read(),...patch});}async saveNote(input:Partial<Note>&{title:string;body:string}){const notes=await this.notes.read(),now=new Date().toISOString(),index=input.id?notes.findIndex(n=>n.id===input.id):-1;const note:Note={id:input.id||randomUUID(),title:input.title,body:input.body,folder:input.folder||'Notes',updatedAt:now};if(index>=0)notes[index]=note;else notes.unshift(note);await this.notes.write(notes);return note;} }

