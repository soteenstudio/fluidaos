import path from 'node:path'; import {randomUUID} from 'node:crypto'; import {JsonRepository} from './jsonRepository.js'; import type {AppDefinition,Note,Notification,Settings,ShellSession} from '../../shared/types/domain.js';
export const defaultSettings:Settings={theme:'dark',accent:'#7c6cff',wallpaper:'aurora',fontScale:1,dockPosition:'bottom',animation:'full',reducedMotion:false,volume:70,brightness:100,notifications:{enabled:true,sound:true,showPreviews:true,doNotDisturb:false}};
export const defaultSession:ShellSession={id:'local',activeWorkspaceId:'workspace-1',pinnedApplications:['files','notes','terminal','settings'],updatedAt:new Date(0).toISOString(),workspaces:[{id:'workspace-1',name:'Main',windows:[],zIndexSequence:10,layout:'freeform'}]};
export class DataService { settings:JsonRepository<Settings>;notes:JsonRepository<Note[]>;notifications:JsonRepository<Notification[]>;session:JsonRepository<ShellSession>;readonly apps:AppDefinition[]=[['files','Files','▰','Browse and manage local files'],['editor','Editor','✎','Edit plain-text documents'],['notes','Notes','◫','Capture persistent notes'],['terminal','Terminal','›_','Run safe OS commands'],['monitor','Monitor','⌁','Inspect this session'],['calculator','Calculator','＋','Calculate with history'],['settings','Settings','⚙','Personalize FluidaOS'],['wallpapers','Wallpapers','◉','Choose a desktop background']].map(([id,name,icon,description])=>({id,name,icon,description}));constructor(root=path.resolve(process.cwd(),'os_storage/.fluida')){this.settings=new JsonRepository(path.join(root,'settings.json'),defaultSettings);this.notes=new JsonRepository(path.join(root,'notes.json'),[]);this.notifications=new JsonRepository(path.join(root,'notifications.json'),[{id:'welcome',title:'Welcome to FluidaOS',message:'Your workspace is ready.',read:false,createdAt:new Date().toISOString()}]);this.session=new JsonRepository(path.join(root,'session.json'),defaultSession);}async saveSettings(patch:Partial<Settings>){return this.settings.write({...await this.settings.read(),...patch,notifications:{...(await this.settings.read()).notifications,...patch.notifications}});}async saveSession(value:ShellSession){if(!value.workspaces.length||!value.workspaces.some(workspace=>workspace.id===value.activeWorkspaceId))throw Object.assign(new Error('Invalid shell session'),{status:400,code:'VALIDATION_ERROR'});return this.session.write({...value,updatedAt:new Date().toISOString()});}async saveNote(input:Partial<Note>&{title:string;body:string}){const notes=await this.notes.read(),now=new Date().toISOString(),index=input.id?notes.findIndex(n=>n.id===input.id):-1;const note:Note={id:input.id||randomUUID(),title:input.title,body:input.body,folder:input.folder||'Notes',updatedAt:now};if(index>=0)notes[index]=note;else notes.unshift(note);await this.notes.write(notes);return note;} }
