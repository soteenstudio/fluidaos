import fs from 'node:fs/promises';
import path from 'node:path';
export class FileService { readonly root:string; constructor(root=path.resolve(process.cwd(),'os_storage')){this.root=path.resolve(root);} resolve(userPath=''){if(typeof userPath!=='string'||path.isAbsolute(userPath)||userPath.includes('\0'))throw Object.assign(new Error('Invalid storage path'),{status:400,code:'INVALID_PATH'});const target=path.resolve(this.root,userPath);if(target!==this.root&&!target.startsWith(`${this.root}${path.sep}`))throw Object.assign(new Error('Path escapes OS storage'),{status:400,code:'INVALID_PATH'});return target;} async list(relative=''){const target=this.resolve(relative);await fs.mkdir(target,{recursive:true});const entries=await fs.readdir(target,{withFileTypes:true});return entries.map(entry=>({name:entry.name,isDirectory:entry.isDirectory(),path:path.posix.join(relative.replaceAll('\\','/'),entry.name)})).sort((a,b)=>Number(b.isDirectory)-Number(a.isDirectory)||a.name.localeCompare(b.name));} async read(relative:string){return fs.readFile(this.resolve(relative),'utf8');} async write(relative:string,content:string){const target=this.resolve(relative);await fs.mkdir(path.dirname(target),{recursive:true});await fs.writeFile(target,content,'utf8');} async mkdir(relative:string){await fs.mkdir(this.resolve(relative),{recursive:false});} async rename(from:string,to:string){const target=this.resolve(to);await fs.mkdir(path.dirname(target),{recursive:true});await fs.rename(this.resolve(from),target);} async delete(relative:string){const target=this.resolve(relative);if(target===this.root)throw Object.assign(new Error('Storage root cannot be deleted'),{status:400,code:'INVALID_PATH'});await fs.rm(target,{recursive:true});} async usage(){let bytes=0,files=0;const walk=async(dir:string):Promise<void>=>{for(const entry of await fs.readdir(dir,{withFileTypes:true}).catch(()=>[])){const item=path.join(dir,entry.name);if(entry.isDirectory())await walk(item);else{bytes+=(await fs.stat(item)).size;files++;}}};await walk(this.root);return{bytes,files};} }

