{
  "name": "browser-os-hybrid",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && cargo run -p fluida-server --release",
    "dev": "npm run build && cargo run -p fluida-server",
    "test": "npm run build && cargo test --workspace"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
