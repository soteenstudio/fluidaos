const manifest={"id":"wallpapers","name":"Wallpapers","icon":"◉","description":"Choose a desktop background"},root=document.querySelector('#app');
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));let sequence=0,pending=new Map();
const call=(operation,payload={})=>new Promise((resolve,reject)=>{const requestId=manifest.id+'-'+(++sequence);pending.set(requestId,{resolve,reject});parent.postMessage({fluida:true,appId:manifest.id,type:'request',requestId,operation,payload},'*')});
window.addEventListener('message',async event=>{if(event.source!==parent||event.data?.fluida!==true)return;if(event.data.type==='response'){const request=pending.get(event.data.requestId);if(request){pending.delete(event.data.requestId);event.data.ok?request.resolve(event.data.data):request.reject(Error(event.data.error))}}if(event.data.type==='init'&&event.data.appId===manifest.id){document.documentElement.dataset.ready='true';try{root.innerHTML='<h1>Wallpapers</h1><p>Choose a preview.</p><div class="wallpapers">'+['aurora','ember','lagoon','dusk'].map(v=>'<button class="wallpaper '+v+'" data-value="'+v+'">'+v+'</button>').join('')+'</div><p id="choice"></p>';root.onclick=e=>{const b=e.target.closest('[data-value]');if(b){document.body.dataset.wallpaper=b.dataset.value;choice.textContent=b.dataset.value+' selected'}};}catch(error){root.innerHTML='<div class="error"><h1>'+manifest.name+'</h1><p>'+esc(error.message)+'</p></div>'}parent.postMessage({fluida:true,appId:manifest.id,type:'ready'},'*')}});
